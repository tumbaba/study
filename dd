#include <iostream>

using namespace std;

int main() {
    int n;

    std::cin >> n   ;
    
    if (n >= 1 && n <= 9) {
        for (int i = 1; i < 10; i++) {
            std::cout << n << "*" << i << "=" << n * i << std::endl;
        }
    }
    else {
        return 0 ;
    }
    return 0;
}
